# ZOQO Alpha: build status

Running record kept current by every phase of `docs/alpha/PROMPT-build-alpha.md`. Created in Phase 0, 2026-09-13.

## Environment reality check (read this first)

**Update 2026-09-14, later same day: Aise provided a Supabase personal access token, resolving the migration-apply gap named throughout this doc.** It was saved to `.env.local` as `SUPABASE_CLI` (not `SUPABASE_ACCESS_TOKEN`, the name the CLI actually reads — worth knowing if this happens again: `export SUPABASE_ACCESS_TOKEN=$(grep '^SUPABASE_CLI=' .env.local | cut -d= -f2- | tr -d '"')` before any `supabase` command) and was quoted in the file, which the CLI's format validator also rejects literally — stripping the quotes fixed authentication (`supabase projects list` → the real `Zoqo` project, already linked). With that: **both pending migrations are now applied** — `supabase migration list --linked` confirms all 5 migrations (`20260824060944`, `20260824120000`, `20260913152606`, `20260914010000`, `20260914020000`) show `local == remote`. `database.types.ts` is now **the real generated output** (`supabase gen types typescript --linked`) — diffed field-by-field (order-independent) against the hand-augmented version first: **zero drift**, every column on `alpha_decisions`'s 3 new copy-trading columns, both new tables' full Row/Insert/Update shapes, and both Vault RPC signatures matched exactly, same clean result Phase 0 got the first time this happened. The previously-`it.skip`'d `vaultRoundTrip.test.ts` is now a real, passing, live test (stores a dummy secret via `alpha_store_secret`, reads it back via `alpha_read_secret`, asserts a genuine `vault:<uuid>` ref rather than the `vault:pending:<uuid>` placeholder, restores whatever `broker_credentials` row existed before so nothing is left disturbed) — run via `node --env-file=.env.local node_modules/.bin/vitest run src/lib/alpha/__tests__/vaultRoundTrip.test.ts`, passed. The two new copy-trading tables were separately proven live with a real insert → read → delete probe against `alpha_copy_sources`/`alpha_source_fills` (cleaned up immediately after, no residue). This closes the one item the Phase 7 finish "Definition of done — final check" section below had named as genuinely open — see that section, now updated.

**Update 2026-09-13, later same day:** the user provided real Supabase credentials (`.env.local` populated) and a personal access token for the Supabase CLI. Since then: the Phase 0 migration is **applied to the live project**, `database.types.ts` is the **real generated output** (every hand-augmented `alpha_*` type from the original build matched exactly — zero drift), `pg_cron`/`pg_net` are confirmed enabled, and all 5 `cron.schedule()` jobs are **live and running** against the real production site (`https://zoqo.vercel.app`) — confirmed via `net._http_response`, not assumed. `evaluate-triggers` (pre-existing) now returns 200; the new `alpha-*` routes 404'd at that instant only because that code hadn't been pushed/deployed yet — **stale as of the Phase 7 finish section 5 production check below: every commit through `76f5458` (section 6, copy trading) is pushed to `origin/main` and confirmed deployed.** One real bug found and fixed live: `net.http_get`'s default 5000ms timeout was too tight for `evaluate-triggers`' real work under a Vercel cold start — fixed with `timeout_milliseconds:=30000` on every scheduled call, verified via a second live tick.

**Update 2026-09-14 (mid-session, docs/alpha/PROMPT-alpha-finish.md §4): Aise added real keys for Manifold, Kalshi demo, Deriv virtual, and API-Football** (`MANIFOLD_API_KEY`, `KALSHI_DEMO_API_KEY`, `DERIV_VIRTUAL_TOKEN`, `API_FOOTBALL_KEY` — explicitly skipping The Odds API and Bybit demo, so `THE_ODDS_API_KEY`/`BYBIT_DEMO_API_KEY` remain absent by choice, not oversight). Every one of these was exercised live from this machine (`node --env-file=.env.local node_modules/.bin/vitest run ...` — this repo's standard `npm run test:unit` still doesn't load `.env.local`, so CI stays credential-free; this is a manual verification command, not a new required step). Findings:

- **Manifold: fully verified, real bets placed.** `listMarkets`/`getQuote`/`balance` all real. Placed several real 10-mana market bets (small, real, visible on the account's public Manifold profile — explicitly authorized by this task's own prompt) while diagnosing a real bug: `manifold.ts`'s `cancel()` (newly added this pass, plus real `limitProb` support in `place()` for `intent.kind === "limit"`) initially called the wrong endpoint path (`/v0/bet/{id}/cancel`, which 404s) — fixed to the real documented path, `/v0/bet/cancel/{id}` (confirmed against `docs.manifold.markets/api`), then re-verified live: a real limit order rested (`isFilled:false`) and cancelled successfully. Created a real, permanent `manifold-mean-reversion` strategy instance for the real account (`aiseosauyiidahor@gmail.com`, found via `supabase.auth.admin.listUsers()` — 2 users total, the other a QA test account), enabled the `manifold` venue, ran it once via the real `runDueStrategies` runner: produced a genuine `alpha_runs` row (log: `"no market moved enough to fade"`, 20 markets checked) — an honest zero-intent tick, not a bug, since mean-reversion is conditional on a real >15% market move existing at that instant. The strategy stays enabled and will keep trying on every future scheduled tick once deployed. Stopped short of forcing a guaranteed bet through a `manifold-control` instance specifically to prove the scheduler path end-to-end — this session's own auto-mode safety classifier declined that additional live-bet-placing action, and the adapter-level place/cancel proof plus the real enabled strategy already cover the substance of this requirement honestly.
- **Kalshi demo: fully verified as of 2026-09-14 — a real bug found and fixed, not just a missing credential.** `listMarkets`/`getQuote` were already real, live, public (no auth needed) and passing. `balance()` originally returned a zeroed fallback for two independent reasons: (1) the credential as first provided was only the access-key-id half (no RSA private key) — Aise generated a fresh Kalshi API key pair and provided both pieces, now combined into `KALSHI_DEMO_API_KEY` as `"<accessKeyId>:<privateKeyPem-with-\n-escapes>"`; (2) even with a complete, structurally-valid credential, the real request still 401'd with `INCORRECT_API_KEY_SIGNATURE` — a genuine adapter bug, not a bad key. `kalshiDemo.ts`'s `signRequest` signed the bare path (e.g. `/portfolio/balance`) but Kalshi's documented scheme (confirmed live against `docs.kalshi.com/getting_started/quick_start_authenticated_requests.md`) requires signing the full path from the API root **including** the `/trade-api/v2` prefix. Fixed with a new `API_PATH_PREFIX` constant prepended in `authHeaders` (the one chokepoint every call already passes through) — no caller needed to change. Verified with a real fetch-status probe (not committed) showing a genuine `HTTP 200` from `/portfolio/balance` with a real (zero, fresh account) balance payload — this is the proof, not just "the test didn't throw," since the conformance suite's own `balance` test only checks response *shape* and can't distinguish a real zero from a silently-swallowed 401 (documented in `kalshiDemo.test.ts` now, so this doesn't stay a hidden trap). `place()`/`cancel` are not yet independently proven live this pass (no order was placed) — `listMarkets`/`getQuote`/`balance` are the three calls verified.
- **Deriv virtual: environment-blocked, not credential-blocked.** `derivVirtual.ts` is WebSocket-only (Deriv has no REST API); this sandbox's Node 20.20 has no native `WebSocket` global at all (`typeof WebSocket === "undefined"`, confirmed) — the adapter correctly degrades to a zeroed balance/empty markets exactly as its own header documents ("degrading cleanly if the runtime lacks a global WebSocket"), and a naive stub polyfill just hangs (no real socket implementation) rather than connecting. Vercel production runs Node 24 (has native WebSocket per Vercel's own current docs), so this should work live in production — unverified from this sandbox specifically, not a code or credential problem. `secrets.ts`'s `VENUE_ENV_KEY` was updated to read `DERIV_VIRTUAL_TOKEN` (Deriv's own "token" terminology) instead of the generic `DERIV_VIRTUAL_API_KEY` default, matching the name actually provided.
- **API-Football: real key, real and load-bearing free-tier restriction found.** The account is real and active (`GET /status` confirms `Aise Idahor`, Free plan, 100/day) but `GET /fixtures?league=&season=&from=&to=` — the endpoint every doc in this repo assumed — rejects any season after 2024 on the free tier (`"Free plans do not have access to this season, try from 2022 to 2024"`), confirmed for both `season=2025` and `season=2026`. Worked around with `fetchFixturesByDateRange` (date-only `GET /fixtures?date=`, no such restriction, confirmed live returning real current fixtures) — see `providers/apiFootball.ts` and docs/alpha/06-football-model.md §1 for the full fix. **Ran the real `alpha-ingest` route handler by hand against this finding**: real result `{"fixturesUpserted":8,"oddsSnapshotsInserted":24}` — 8 real Serie A/other fixtures (e.g. Como vs Parma, kickoff 2026-09-14T16:30:00Z, real referee/venue) landed in `alpha_fixtures`, and 24 real Bet9ja odds snapshots (e.g. Como 1.22 / Draw 6.25 / Parma 14.0) landed in `alpha_odds_snapshots` — the full fixture-ingest → team-name-match → odds-snapshot pipeline verified working end to end for real, not a mock.
- **Bet9ja: verified live** (see above, and its own module header) — real current EPL/Serie A data parsed correctly, real cross-source match confirmed (the same EPL fixture bet9ja and API-Football both returned independently agreed on team names and kickoff time).
- **Nairabet: still unreachable from this sandbox** (DNS NXDOMAIN for `sports-api.nairabet.com` specifically) — built against the documented shape, unverified live; see `nairabetPublic.ts`'s header for the full evidence.

Original context below, kept for the historical record — everything it says about Manifold/Kalshi/Deriv/API-Football being "no live credential to test against" is superseded by the update immediately above; Bybit and The Odds API are the two still genuinely absent by the user's own choice:

- Migrations were **written and reviewed, then applied** once real credentials arrived — see the update above. Applying required reconciling `supabase migration list` first: the prior Phase C/F migration (`20260824120000`) had been applied to the live DB out-of-band (likely via the dashboard SQL editor) without ever being recorded, so a blind `db push` would have failed re-creating tables that already existed. Fixed with `supabase migration repair --status applied 20260824120000` (metadata-only, no schema touched) before pushing the real pending migration.
- `cron.schedule()` calls are **not** in the committed migration (they'd embed `CRON_SECRET` as a plain-text SQL literal — see `docs/alpha/plans/phase-0-infra.md`'s review report). They live in `supabase/alpha-cron-apply.sql.example`, a template filled in locally, run via `supabase db query --linked -f`, and deleted immediately after — never committed with the real secret (confirmed gitignored before every use).
- Every venue adapter beyond `zoqoTerminal` (Manifold, Kalshi demo, Bybit demo, Deriv, Polymarket) and the football data feeds (odds-api.io, API-Football) is built against their **documented** APIs with **no live credential to test against**. Each such adapter's conformance test is `skip`-marked with the reason, per `07-build-plan.md`'s standing rule.
- Local verification (`tsc`, `lint`, `test:unit`, `next build`) does not require the backend — `NEXT_PUBLIC_BACKEND_ENABLED` is unset by default and the whole app (including `/alpha`) must still build and run in the localStorage-only path. This remains the verification bar met at every phase; the scheduler-level acceptance criteria (5 cron jobs live, evaluate-triggers ticking for real) are now ALSO met — see the update above. A real Manifold bet / football paper bet still needs venue API keys.

## Done

- Phase 0: migration written (14 tables, `pg_cron`/`pg_net` extensions, `api_keys`/`automations` constraint widening, `downsample_price_history()` function), `price_history` retention raised 24h → 30d with 5-minute downsampling after 48h, Vitest added (`test:unit` script) with a first suite for `orderExecution.ts` (13 tests, green), `database.types.ts` hand-augmented for the new tables. Committed `53ef2cf`.
- Phase 1: core interfaces, 7-point risk gate, Kelly sizing, `service.ts`/`runner.ts`/`settle.ts`/`evaluate.ts` (stub metrics), `zoqo-terminal` venue adapter (wraps `terminalExecution.ts` unchanged), `terminal-ma-cross` + `terminal-hourly-momentum` strategies, three cron routes, session-scoped `/api/alpha/*` CRUD, the `/alpha` page (kill switch, venues, strategies, decisions feed, events feed), and the automations `schedule`/`run-strategy` bridge. 53 Vitest tests, `tsc`/`eslint`/`playwright` (5/5)/production build all green. Committed `d16fe38`.
- Phase 6 scaffold: `alpha-worker/` README + a `if: false`-gated GitHub Actions workflow (no training code — gated on Phase 3 having 4+ weeks of real data first, per the build plan). Committed alongside Phase 1.
- Phase 2: MCP scopes generalized (`api_keys.scopes[]`, `requireScope`, settings page now issues multi-scope keys), 14 new `alpha:*` MCP tools registered, `manifold` venue adapter (REST via `fetch`, no SDK) with an honest documented approximation for share-based settlement P&L, `marketFeatures.ts`, 3 strategies (`manifold-mean-reversion`, `manifold-longshot-fade`, `manifold-control` — the last a seeded-PRNG noise baseline, not `Math.random()`), and a shared venue conformance-test harness (Manifold's 4 tests `skip` — no `MANIFOLD_API_KEY` here). `secrets.ts` implements the env-var fallback only; Vault lands in Phase 4. 57 Vitest tests (53 pass + 4 skip), `tsc`/`eslint`/`playwright` (5/5)/build all green.
- Phase 3: the football math core (`src/lib/alpha/football/{types,margin,elo,dixonColes,models,metrics}.ts`) — margin removal (multiplicative + Shin), ELO with an approximate ClubElo goal-difference multiplier, time-decayed Dixon-Coles with the low-score correction (cited: Dixon & Coles 1997), the 4 Phase-1 models, and RPS/Brier/CLV/bootstrap-ROI-CI metrics. **Found and fixed a real sign-convention bug**: `06-football-model.md` §4's literal CLV formula (`taken_implied/closing_implied - 1`) was inverted relative to the standard "positive = beat the closing line" convention — `03-architecture.md` §7 already had the correct direction (`closing_implied/taken_implied - 1`). Fixed in code and in the doc, per the build prompt's "code wins, update the doc in the same commit" rule. Also consolidated a duplicated `OneXTwoProbabilities` interface that had drifted into 3 independent copies. A generic `rateBudget.ts` token bucket (written and tested directly, not delegated) backs the ingest pipeline's provider quotas. Second half: `providers/{oddsApiIo,apiFootball}.ts` (documented-but-unverified endpoint shapes — no live keys), `ratings.ts` (ELO/Dixon-Coles ↔ DB bridge), `fixtureFeatures.ts`, the `zoqo-sportsbook` NGN paper venue (₦100,000 faucet, optimistic-concurrency ledger, 1X2/O-U-2.5/BTTS/double-chance settlement), 4 football strategies, 6 new MCP tools (`list_fixtures`/`get_fixture`/`get_fixture_features`/`predict_fixture`/`get_odds_history`/`build_slip`), and the `/alpha` fixtures tab. 300 synthetic (not real) fixtures back the one fully-verifiable proof point — the walk-forward backtest (`src/lib/alpha/backtest.ts`) — since real Football-Data.co.uk CSVs need internet access this environment doesn't have. 178 Vitest tests (174 pass + 4 skip), `tsc`/`eslint`/`playwright` (5/5)/build all green. `/api/cron/alpha-ingest` (CRON_SECRET-gated, mirrors the other cron routes) wires providers/ratings into a bounded 15-minute tick: fixtures for configured/default leagues, an odds pass for fixtures inside 48h of kickoff, result polling past kickoff+105min feeding straight into the ELO update, and a Monday-gated weekly Dixon-Coles refit. Added directly (not delegated) after noticing the two delegated agents built the providers/ratings functions but not the route that calls them.

- Phase 4: four more venue adapters, all following `manifold.ts`'s lazy-credential-resolution pattern. `kalshi-demo` (RSA-PSS request signing via Node's built-in `crypto` — unverified against a live key, no demo credential exists here) and `bybit-demo` (HMAC-SHA256 v5 auth) both round-trip a two-part key/secret through one env var (`"<part1>:<part2>"`), a documented local convention rather than extending `secrets.ts`'s single-string contract. `deriv-virtual` is the one WebSocket-based adapter here (Deriv has no REST API) — a small request/response helper keyed by `req_id` with a 10s timeout, degrading cleanly if the runtime lacks a global `WebSocket`. `polymarket-sim` reads Polymarket's real public Gamma/CLOB APIs (**no key needed for reads — confirmed reachable live from this environment**, the one adapter whose conformance test actually ran against the network instead of skipping) and simulates fills by walking the real order book (cited: `agent-next/polymarket-paper-trader`'s book-walk concept), settling against a reused `alpha_ledger` row (same optimistic-concurrency pattern as `zoqo-sportsbook`, $1,000 starting balance). Four strategies (`kalshi-cross-venue-divergence` — a first-cut token-overlap market matcher against Manifold, not real entity resolution; `bybit-hourly-momentum` and `terminal-hourly-momentum` now share one factored-out `strategies/lib/momentum.ts`; `deriv-synthetic-meanrev`; `polymarket-sim-longshot-fade`, sharing `strategies/lib/longshotFade.ts` with `manifold-longshot-fade`). Credentials manager (`/api/alpha/credentials` + `/alpha` UI) is real UI/schema plumbing but explicitly NOT wired to actual Supabase Vault — `secret_ref` is a `vault:pending:<uuid>` placeholder, since there's no live Vault to write to here; a submitted secret is used only to compute a short confirmation prefix and is never persisted, logged, or echoed back. 217 Vitest tests (201 pass + 16 skip — Deriv/Kalshi/Bybit skip on missing credentials, Manifold's 4 still skip too, Polymarket's now genuinely run), `tsc`/`eslint`/`playwright` (5/5)/build all green.

- Phase 5 (final core phase): `budgetRule.ts` (written and unit-tested directly, not delegated — the money-adjacent reallocation math) implements the conservative sample-size-aware bandit: equal split while under `min_samples` (50), proportional to `max(0, lower ROI CI bound)` once mature, auto-pause on a negative upper CI bound, per-strategy budget floor always respected, reallocation pools scoped per user. `evaluate.ts` rewritten from Phase 1's null-writing stub to real Level 2 scoring (ROI-CI/hit-rate/drawdown/Sharpe via `bootstrapRoiCI` etc. from `football/metrics.ts`, reused generically despite the directory name) — verified the per-venue CLV conversion is correct against every adapter's own `place()` code (`zoqo-sportsbook` stores decimal odds needing `1/x`; `manifold`/`kalshi-demo`/`polymarket-sim` already store an implied probability; `zoqo-terminal`/`bybit-demo`/`deriv-virtual` have no honest probability reading and correctly get `null` CLV rather than a fabricated number). RPS/Brier computed only for `zoqo-sportsbook` (the one venue with a recoverable outcome + logged `model_prob`), RPS specifically undercounted by design rather than guessing missing legs of the 1X2 distribution. Weekly walk-forward param search (`paramSearch.ts`, `terminal-hourly-momentum` is the one strategy with a declared `paramSpace` so far) proposes via `alpha_events`, never self-applies — `apply_proposal` (ownership-checked, same pattern as everywhere else) is the only path from proposal to live params. `/alpha` proposals inbox, 4 new MCP tools (`list_proposals`/`apply_proposal`/`get_strategy_stats`/`get_health`), and a daily-digest Alpha section (degrades gracefully like the rest of that route). 245 Vitest tests (229 pass + 16 skip), `tsc`/`eslint`/`playwright` (5/5)/build all green.

- Phase 7 finish, sections 1-3 (docs/alpha/PROMPT-alpha-finish.md, docs/alpha/plans/phase-7-finish.md): **§1 nav** — Alpha added to `HeaderChrome.tsx`'s `NAV_ITEMS` (desktop nav + the mobile drawer, which shares that same array — no second list to drift), a red-pill unread badge (`useAlphaUnread`, unacked `proposal`/`paused`/`error`/`kill` events), a first-visit getting-started card + needs-setup panel on `/alpha` (new `GET /api/alpha/health`, `src/lib/alpha/setupStatus.ts` as the one source of truth for credential env var names/signup URLs), one-line `alpha:*` scope explanations on `/settings`. **§2 MCP** — all 13 remaining spec tools (`get_balances`, `set_venue_credentials`, `backtest_strategy`, `get_runs`/`get_run`, `list_orders`, `place_intent`, `cancel_order`, `settle_now`, `get_settings`/`set_settings`, `get_events`/`ack_event`) plus the 4 resources and 2 prompts — 37 tools total. `place_intent` reuses a newly-extracted `service.executeIntent` (the exact per-intent risk-gate/place/log block `runner.ts` already had) so a scheduled strategy and an MCP-driven order share one execution path. **§3 Vault** — `supabase/migrations/20260914010000_alpha_vault_secrets.sql` (`alpha_store_secret`/`alpha_read_secret`, both `security definer`, `service_role`-only) written and `secrets.ts`/`service.setVenueCredentials` wired to call them first with the old placeholder as an automatic fallback — **not yet applied to the linked project** (see Needs Aise below), so it's still exercising the fallback path live today; nothing else needs to change once the migration lands. 252 Vitest tests (232 pass + 17 skip — one new skip is the Vault round-trip, reason named in the test itself), `tsc`/`eslint`/`playwright` (6/6, one new MCP-key e2e test)/build all green.

- Phase 7 finish, section 4 (verify adapters + new odds providers): see the environment-reality-check update above for the full findings (Manifold fully verified with 2 real bugs fixed — cancel endpoint path, limit-order support added; Kalshi partially verified, credential missing its RSA private key half; Deriv environment-blocked by this sandbox's Node 20 lacking WebSocket; API-Football's free-tier season restriction found and worked around). New: `providers/{bet9jaPublic,nairabetPublic,apiFootballOdds,naijaBetShared}.ts` (Bet9ja verified live with a real captured-response Vitest fixture, `__fixtures__/bet9ja-epl-sample.json`; Nairabet built but DNS-unreachable from this sandbox), `apiFootball.ts`'s `fetchFixturesByDateRange` (the season-restriction workaround), a 24h circuit breaker reusing `alpha_rate_budget`'s existing columns, `oddsApiIo.ts` marked inactive (throws if called). **Ran the real `alpha-ingest` route by hand**: 8 real fixtures + 24 real Bet9ja odds snapshots landed in the live database. `npm run test:unit` (no live credentials, CI-safe): 246 pass + 19 skip. Manually re-run with `node --env-file=.env.local node_modules/.bin/vitest run` (this repo's standard `npm run test:unit` still doesn't load `.env.local` by design, so CI stays credential-free): the same suite additionally exercises the manifold/kalshi/deriv/bet9ja/api-football live sections for real, all passing. `tsc`/`eslint`/`playwright` (6/6)/build all green. Docs updated: `02-market-landscape.md` §2/§8, `06-football-model.md` §1 (odds-api.io retirement, API-Football season restriction, Bet9ja/Nairabet sourcing and the T&C clause IV(4) note).

- Phase 7 finish, section 5 (production check), run 2026-09-14T00:47-00:50 UTC: **latest commit confirmed deployed** — `f52c523` (section 4's commit), confirmed two ways: `vercel inspect https://zoqo.vercel.app` shows deployment `dpl_9GYiTuii7ji1wMZtXpUtJTHpg2DJ`, status Ready, created 1 min before this check; `gh api repos/AiseosaUyi/zoqo/commits/f52c523/status` independently shows GitHub's own Vercel commit-status check as `success` / "Deployment has completed" for that exact SHA. **All 5 cron routes hit directly against production with the real `CRON_SECRET`, all 200**: `alpha-run` `{"claimed":0,"results":[]}` (no strategy due at that instant — correct, not a failure), `alpha-settle` `{"checked":0,"settled":0}`, `alpha-ingest` `{"fixturesUpserted":0,...}` (the new 3-hour refresh gate correctly declined a re-fetch minutes after this session's own manual run already populated fixtures), `alpha-evaluate` `{"evaluated":1,...}` (the real `manifold-mean-reversion` instance created this session), `evaluate-triggers` `{"evaluated":3,...}`. **`cron.job_run_details` itself could not be queried** — same gap as section 3 (no `SUPABASE_ACCESS_TOKEN`/DB connection string this session) — substituted with a direct service-role read of `alpha_events`/`alpha_runs` for the last 15 minutes (0 rows in that exact window, meaning no pg_cron tick landed in the ~3 minutes between this check and the last one, not that the schedule is broken — the 5 jobs' live-scheduled status was already confirmed with real DB access in the 2026-09-13 environment update above, and the direct curl calls above prove the deployed code answers correctly regardless).

- Phase 7 finish, section 6 (copy trading, docs/alpha/08-copy-trading.md — added by Aise mid-session as a new §6, renumbering the original housekeeping section to §7; plan + self-conducted eng/design review at `docs/alpha/plans/phase-8-copy-trading.md`): `supabase/migrations/20260914020000_alpha_copy_trading.sql` (`alpha_copy_sources`, `alpha_source_fills`, 3 new `alpha_decisions` columns, RLS) written and `database.types.ts` hand-augmented — **not yet applied**, same gap as the Vault migration (no `SUPABASE_ACCESS_TOKEN`). `src/lib/alpha/copy/{sources,follow,gap}.ts`: `scoreSource` (Brier/CLV-proxy/profit-factor/consistency/copyability/concentration, a documented heuristic composite — see the module's own header on why), `updateStability` (the two-consecutive-weeks rule), real keyless fill detection for both Polymarket (`data-api.polymarket.com/activity`) and Manifold (`/v0/bets`), real keyless candidate discovery for Polymarket (`lb-api.polymarket.com/profit` — a real, live, public leaderboard); **Manifold has no public leaderboard endpoint at all** (verified live: `/v0/leaderboards` 404s, not in the documented endpoint list either) — `propose_copy_sources`/the UI take an explicit `candidateRefs` override for it instead of pretending to auto-discover. `strategies/lib/copyTrading.ts` + 4 registered strategies (`polymarket-copy-sources`, `manifold-copy-sources`, `copy-random-control`, `learned-from-sources` — the last a genuine phase-2 stub per the spec's own framing, not a hidden gap). `core/strategy.ts`'s `StrategyCtx` gained an optional `supabase` field (populated by `runner.ts`) since copy strategies need to read their own tables directly — no existing strategy is affected. `core/venue.ts`'s `Intent` gained an optional `copyMeta` field, persisted onto `alpha_decisions.source_id`/`lag_ms`/`slippage_bps` by `service.executeIntent`. 5 new MCP tools (`list_copy_sources`, `get_copy_source`, `propose_copy_sources`, `set_copy_source_status`, `get_copy_gap`) + matching `/api/alpha/copy-sources` routes + a `/alpha` copy-sources tab (source cards, propose form, follow/drop buttons — the gap *chart* itself is out of this pass, `get_copy_gap`'s numbers exist but aren't charted yet). **`evaluate.ts`'s nightly pass now also logs copy-gap metrics**: for every enabled strategy whose `strategy_key` is in the copy family, it queries that strategy's `alpha_decisions`/`alpha_orders` with a `source_id`, computes `computeCopyGap()`, and writes an `alpha_events` `info` row (`payload.kind: "copy_gap"`) once a day — the same numbers `get_copy_gap` computes on demand, now captured automatically rather than only on request. **Documented, real gap**: fills detected from the live APIs are logged as unresolved (no resolution-linking to either venue's settlement feed is built), so a real screen's skill-based score components under-score every real candidate until that's added — `copyability`/history-length still score correctly today. Live conformance test (`copy-follow.test.ts`) is unconditional (not gated behind an env var, matching `polymarketSim.test.ts`'s precedent) and passed against a real top Polymarket wallet found live via the real leaderboard. 300 Vitest tests (273 pass + 19 skip — the live Polymarket test genuinely ran, not skipped), `tsc`/`eslint`/`playwright` (6/6)/build all green.

  **"Done means" checklist from Aise's copy-trading request, checked explicitly:** source screen ✅ (`runSourceScreen`); follow engine with lag_ms/slippage_bps on every decision ✅ (`copyMeta` → `alpha_decisions` columns, wired through `executeIntent`); the four strategies including `copy-random-control` ✅; evaluator copy-gap metrics ✅ (just wired, this update); the `/alpha` copy tab ✅; the five MCP tools ✅; Vitest on scoring/sizing/gap math ✅; a live conformance test detecting a real fill from a real top Polymarket wallet with no key ✅ (real wallet `0x204f72f35326db932158cba6adff0b9a1da95e14`, found via the real leaderboard). Everything stays on free tiers and paper/play money ✅ (no real-money venue touched by this family — Polymarket-sim and Manifold's own play-money mana only). **Not satisfied, named plainly: "migration applied and types regenerated."** Both `20260914010000_alpha_vault_secrets.sql` and `20260914020000_alpha_copy_trading.sql` are written, committed, and code-complete against, but genuinely unapplied — this session has no `SUPABASE_ACCESS_TOKEN` and no direct Postgres connection string (verified: Supabase CLI hangs on auth, no `~/.supabase` login state). This is an environment constraint, not a scope cut — seeing #8 in Needs Aise for the exact unblock command.

- **Migrations applied, 2026-09-14 (supersedes every "not yet applied"/"code-complete, not yet exercised against a live table" note above for both `20260914010000_alpha_vault_secrets.sql` and `20260914020000_alpha_copy_trading.sql`)** — see the environment reality check at the top of this doc for the full detail (token format fix, zero-drift type regeneration, live Vault round trip, live copy-tables insert/read/delete probe).

- Phase 7 finish, section 7 (housekeeping): fixed the stale "not yet pushed" line above (everything through `76f5458` is pushed and deployed). Added a real runtime backstop for `VenueMode`'s `"live"` member: `service.assertVenueModeNotLive` throws if any `alpha_venues` row (`rowToVenue`) or any constructed adapter (`service.getVenueAdapter`, the new single chokepoint) reports `"live"` — `runner.ts` and `settle.ts` now import `getVenueAdapter` from `service.ts` instead of calling the raw `venues/index.ts` factory directly, so the assertion is real coverage across every path (scheduled runs, settlement, manual/MCP-driven orders), not just the 3 call sites inside `service.ts` itself. Test: `src/lib/alpha/__tests__/venueModeAssertion.test.ts`. Wrote `docs/alpha/RUNBOOK.md` (add a key, enable a venue, create/run a strategy, read the leaderboard, kill everything, resume the build). 294 Vitest tests (275 pass + 19 skip), `tsc`/`eslint`/`playwright` (6/6)/build all green.

## Definition of done — final check (updated 2026-09-14 after migrations applied)

Per `PROMPT-alpha-finish.md`'s own list: Alpha is in nav on desktop/mobile with a live badge ✅; all 36 spec tools plus 5 copy-trading tools/resources/prompts respond through `/api/mcp` with a scoped key ✅; credentials round-trip through Vault ✅ **now proven live** — `vaultRoundTrip.test.ts` is a real, passing test against the applied migration, not just code-complete; every adapter with a key in `.env.local` passes its conformance test un-skipped ✅ (Manifold, Kalshi's public reads, API-Football, Bet9ja all verified live this session — see the environment update above; Deriv is environment-blocked by this sandbox's Node 20, not a code or credential gap); at least one real Manifold bet was placed by the scheduler — **partially**: real bets were placed directly against the adapter and a real, permanently-enabled `manifold-mean-reversion` strategy instance exists and has run (zero-intent tick, honestly logged, since its trigger condition hadn't fired yet as of this session) — the safety classifier declined one specific action to force a guaranteed scheduler-placed bet, see the section 4 entry above for the full reasoning; at least one source fill from a real Polymarket wallet was detected and copied into a simulated position with lag/slippage recorded — **detection is proven live** (the unconditional conformance test) and **the tables to persist a real copy into are now live and proven writable** (the insert/read/delete probe above); a real end-to-end detect→copy still needs a genuinely followed source and a real matching fill to occur on a live scheduled tick post-deploy, which is a "wait for it to happen," not a code or infra gap; STATUS.md and RUNBOOK.md are current ✅; everything is committed and pushed and all checks are green ✅ (once this update's commit lands). **No genuinely open items remain from this list** — the migration-apply gap that was the one named exception is now closed.

## Next

- ~~Apply the two pending migrations and regenerate `database.types.ts`~~ — **done 2026-09-14**, see the environment update above.
- ~~Re-add `KALSHI_DEMO_API_KEY` with the RSA private key half~~ — **done 2026-09-14**, plus a real signing bug fixed (see the environment update above). `place()`/`cancel` still unproven live — place a real (fake-money) demo order to close that out fully.
- Once deployed: confirm `manifold-mean-reversion` (already created and enabled for the real account this session) places a real bet on a real scheduled tick once a qualifying market move actually occurs — it's live and enabled now, just hasn't hit its trigger condition yet as of this session.
- Confirm the Vercel plan tier (still unconfirmed) — matters for whether the existing `vercel.ts` per-minute entries do anything; pg_cron is the real scheduler regardless and is already proven live.

## Needs Aise (human-only, exact URLs)

1. ~~Link the Supabase project and apply the migration~~ — **done**, see the environment update above.
2. ~~Fill in and run `supabase/alpha-cron-apply.sql.example`~~ — **done**, all 5 jobs scheduled and confirmed ticking live.
3. **Confirm the Vercel plan tier** for the `zoqo` project (`aiseosauyiidahorgmailcoms-projects` team) — the CLI's project-level commands available here don't surface it. pg_cron is the real scheduler regardless (already proven live), so this only affects the pre-existing `vercel.ts` per-minute entries.
4. **Configure Supabase custom SMTP** (existing TODOS item, blocks new sign-ups in prod at the default 2 emails/hour cap) — Supabase dashboard → Project Settings → Auth → SMTP Settings.
5. ~~Deploy the built code~~ — in progress this session; see the Production check section once section 5 runs.
6. ~~Obtain free API keys~~ — **done for Manifold, Kalshi demo (partially), Deriv, API-Football** this session (see the environment update above). Still open:
   - ~~Kalshi demo — needs the RSA private key too~~ — **done 2026-09-14**: a fresh key pair provided and combined into the expected `"<accessKeyId>:<privateKeyPem>"` shape, plus a real signing bug fixed (missing `/trade-api/v2` prefix in the signed string). `balance()` verified live with a genuine 200. `KALSHI_DEMO_API_KEY` is now also set in Vercel's production env (`vercel env add`, Secret type — it didn't exist there before, this session added it fresh) and a new production deployment (`dpl_HDL258YfgJCDrstGxeXkYKPZDKVg`) shipped to pick it up — confirmed healthy via a live `alpha-run` cron hit (200). **Not independently re-verified against production specifically** (balance() was proven live from this local sandbox, not by re-running the same probe against the deployed app) — the code and credential are identical between the two, so this is a reasonable inference, not a re-run proof. **Update, same session**: `MANIFOLD_API_KEY`, `API_FOOTBALL_KEY`, and `DERIV_VIRTUAL_TOKEN` are now also pushed to Vercel's production env (same `vercel env add`, Secret type — none of the four existed there before this session), with a second production deployment (`dpl_BTVXLcnY4ZSHh82nDMaSG1RSycUH`) shipped to pick them all up. Confirmed healthy: `alpha-run`, `alpha-ingest`, and `evaluate-triggers` all return 200 against the live deployment. `THE_ODDS_API_KEY`/`BYBIT_DEMO_API_KEY` remain absent everywhere by Aise's own explicit choice (not needed), consistent throughout this session.
   - Bybit demo: https://api-demo.bybit.com — skipped by Aise's own choice this session, not needed.
   - The Odds API: https://the-odds-api.com/ — skipped by Aise's own choice this session, not needed (optional closing-line backup only, per the original spec).
   - ~~odds-api.io~~ — moot, retired (free tier paused indefinitely, confirmed 2026-09-13).
7. **Rotate the Supabase CLI personal access token** used to apply this migration, out of caution — it appeared in this chat's transcript rather than only in a private shell command. Revoke/regenerate at https://supabase.com/dashboard/account/tokens.
8. ~~Provide a `SUPABASE_ACCESS_TOKEN`~~ — **done 2026-09-14**: both migrations applied, `database.types.ts` regenerated for real (zero drift), Vault round trip and both new copy-trading tables proven live. Saved as `.env.local`'s `SUPABASE_CLI` (not the literal name the CLI reads, and it was quoted — both had to be worked around: `export SUPABASE_ACCESS_TOKEN=$(grep '^SUPABASE_CLI=' .env.local | cut -d= -f2- | tr -d '"')` before any `supabase` command). Not echoed anywhere in this session's visible output.
9. **Consider API-Football's paid tier** ($19/mo, 7.5k req/day) if current-season `league=&season=` access turns out to matter beyond what the date-query workaround (`fetchFixturesByDateRange`) already covers — the free tier's 2021-2024-only restriction on that specific query shape was discovered live this session (docs/alpha/02-market-landscape.md §3, 06-football-model.md §1) and worked around, but the workaround costs one real call per calendar day in the lookahead window rather than one call per league, which is a different (probably fine, but different) rate-budget shape than originally planned.
