# ZOQO Alpha: build status

Running record kept current by every phase of `docs/alpha/PROMPT-build-alpha.md`. Created in Phase 0, 2026-09-13.

## Environment reality check (read this first)

**Update 2026-09-13, later same day:** the user provided real Supabase credentials (`.env.local` populated) and a personal access token for the Supabase CLI. Since then: the Phase 0 migration is **applied to the live project**, `database.types.ts` is the **real generated output** (every hand-augmented `alpha_*` type from the original build matched exactly — zero drift), `pg_cron`/`pg_net` are confirmed enabled, and all 5 `cron.schedule()` jobs are **live and running** against the real production site (`https://zoqo.vercel.app`) — confirmed via `net._http_response`, not assumed. `evaluate-triggers` (pre-existing) now returns 200; the new `alpha-*` routes correctly 404 because that code is committed locally but **not yet pushed to git / deployed to Vercel** (the build prompt says not to push without being told to — see "Needs Aise" #1 below, now the actual next step). One real bug found and fixed live: `net.http_get`'s default 5000ms timeout was too tight for `evaluate-triggers`' real work under a Vercel cold start — fixed with `timeout_milliseconds:=30000` on every scheduled call, verified via a second live tick.

Still true for everything venue-API-shaped below: no Manifold/Kalshi/Bybit/Deriv/odds-api.io/API-Football keys exist yet, so those adapters remain unverified against live traffic (Polymarket's public reads are the one exception, confirmed reachable). Original context, kept for the historical record:

- Migrations were **written and reviewed, then applied** once real credentials arrived — see the update above. Applying required reconciling `supabase migration list` first: the prior Phase C/F migration (`20260824120000`) had been applied to the live DB out-of-band (likely via the dashboard SQL editor) without ever being recorded, so a blind `db push` would have failed re-creating tables that already existed. Fixed with `supabase migration repair --status applied 20260824120000` (metadata-only, no schema touched) before pushing the real pending migration.
- `cron.schedule()` calls are **not** in the committed migration (they'd embed `CRON_SECRET` as a plain-text SQL literal — see `docs/alpha/plans/phase-0-infra.md`'s review report). They live in `supabase/alpha-cron-apply.sql.example`, a template filled in locally, run via `supabase db query --linked -f`, and deleted immediately after — never committed with the real secret (confirmed gitignored before every use).
- Every venue adapter beyond `zoqoTerminal` (Manifold, Kalshi demo, Bybit demo, Deriv, Polymarket) and the football data feeds (odds-api.io, API-Football) is built against their **documented** APIs with **no live credential to test against**. Each such adapter's conformance test is `skip`-marked with the reason, per `07-build-plan.md`'s standing rule.
- Local verification (`tsc`, `lint`, `test:unit`, `next build`) does not require the backend — `NEXT_PUBLIC_BACKEND_ENABLED` is unset by default and the whole app (including `/alpha`) must still build and run in the localStorage-only path. This remains the verification bar met at every phase; the scheduler-level acceptance criteria (5 cron jobs live, evaluate-triggers ticking for real) are now ALSO met — see the update above. A real Manifold bet / football paper bet still needs venue API keys.

## Done

- Phase 0: migration written (14 tables, `pg_cron`/`pg_net` extensions, `api_keys`/`automations` constraint widening, `downsample_price_history()` function), `price_history` retention raised 24h → 30d with 5-minute downsampling after 48h, Vitest added (`test:unit` script) with a first suite for `orderExecution.ts` (13 tests, green), `database.types.ts` hand-augmented for the new tables. Committed `53ef2cf`.
- Phase 1: core interfaces, 7-point risk gate, Kelly sizing, `service.ts`/`runner.ts`/`settle.ts`/`evaluate.ts` (stub metrics), `zoqo-terminal` venue adapter (wraps `terminalExecution.ts` unchanged), `terminal-ma-cross` + `terminal-hourly-momentum` strategies, three cron routes, session-scoped `/api/alpha/*` CRUD, the `/alpha` page (kill switch, venues, strategies, decisions feed, events feed), and the automations `schedule`/`run-strategy` bridge. 53 Vitest tests, `tsc`/`eslint`/`playwright` (5/5)/production build all green. Committed `d16fe38`.
- Phase 6 scaffold: `alpha-worker/` README + a `if: false`-gated GitHub Actions workflow (no training code — gated on Phase 3 having 4+ weeks of real data first, per the build plan). Committed alongside Phase 1.
- Phase 2: MCP scopes generalized (`api_keys.scopes[]`, `requireScope`, settings page now issues multi-scope keys), 14 new `alpha:*` MCP tools registered, `manifold` venue adapter (REST via `fetch`, no SDK) with an honest documented approximation for share-based settlement P&L, `marketFeatures.ts`, 3 strategies (`manifold-mean-reversion`, `manifold-longshot-fade`, `manifold-control` — the last a seeded-PRNG noise baseline, not `Math.random()`), and a shared venue conformance-test harness (Manifold's 4 tests `skip` — no `MANIFOLD_API_KEY` here). `secrets.ts` implements the env-var fallback only; Vault lands in Phase 4. 57 Vitest tests (53 pass + 4 skip), `tsc`/`eslint`/`playwright` (5/5)/build all green.
- Phase 3: the football math core (`src/lib/alpha/football/{types,margin,elo,dixonColes,models,metrics}.ts`) — margin removal (multiplicative + Shin), ELO with an approximate ClubElo goal-difference multiplier, time-decayed Dixon-Coles with the low-score correction (cited: Dixon & Coles 1997), the 4 Phase-1 models, and RPS/Brier/CLV/bootstrap-ROI-CI metrics. **Found and fixed a real sign-convention bug**: `06-football-model.md` §4's literal CLV formula (`taken_implied/closing_implied - 1`) was inverted relative to the standard "positive = beat the closing line" convention — `03-architecture.md` §7 already had the correct direction (`closing_implied/taken_implied - 1`). Fixed in code and in the doc, per the build prompt's "code wins, update the doc in the same commit" rule. Also consolidated a duplicated `OneXTwoProbabilities` interface that had drifted into 3 independent copies. A generic `rateBudget.ts` token bucket (written and tested directly, not delegated) backs the ingest pipeline's provider quotas. Second half: `providers/{oddsApiIo,apiFootball}.ts` (documented-but-unverified endpoint shapes — no live keys), `ratings.ts` (ELO/Dixon-Coles ↔ DB bridge), `fixtureFeatures.ts`, the `zoqo-sportsbook` NGN paper venue (₦100,000 faucet, optimistic-concurrency ledger, 1X2/O-U-2.5/BTTS/double-chance settlement), 4 football strategies, 6 new MCP tools (`list_fixtures`/`get_fixture`/`get_fixture_features`/`predict_fixture`/`get_odds_history`/`build_slip`), and the `/alpha` fixtures tab. 300 synthetic (not real) fixtures back the one fully-verifiable proof point — the walk-forward backtest (`src/lib/alpha/backtest.ts`) — since real Football-Data.co.uk CSVs need internet access this environment doesn't have. 178 Vitest tests (174 pass + 4 skip), `tsc`/`eslint`/`playwright` (5/5)/build all green. `/api/cron/alpha-ingest` (CRON_SECRET-gated, mirrors the other cron routes) wires providers/ratings into a bounded 15-minute tick: fixtures for configured/default leagues, an odds pass for fixtures inside 48h of kickoff, result polling past kickoff+105min feeding straight into the ELO update, and a Monday-gated weekly Dixon-Coles refit. Added directly (not delegated) after noticing the two delegated agents built the providers/ratings functions but not the route that calls them.

- Phase 4: four more venue adapters, all following `manifold.ts`'s lazy-credential-resolution pattern. `kalshi-demo` (RSA-PSS request signing via Node's built-in `crypto` — unverified against a live key, no demo credential exists here) and `bybit-demo` (HMAC-SHA256 v5 auth) both round-trip a two-part key/secret through one env var (`"<part1>:<part2>"`), a documented local convention rather than extending `secrets.ts`'s single-string contract. `deriv-virtual` is the one WebSocket-based adapter here (Deriv has no REST API) — a small request/response helper keyed by `req_id` with a 10s timeout, degrading cleanly if the runtime lacks a global `WebSocket`. `polymarket-sim` reads Polymarket's real public Gamma/CLOB APIs (**no key needed for reads — confirmed reachable live from this environment**, the one adapter whose conformance test actually ran against the network instead of skipping) and simulates fills by walking the real order book (cited: `agent-next/polymarket-paper-trader`'s book-walk concept), settling against a reused `alpha_ledger` row (same optimistic-concurrency pattern as `zoqo-sportsbook`, $1,000 starting balance). Four strategies (`kalshi-cross-venue-divergence` — a first-cut token-overlap market matcher against Manifold, not real entity resolution; `bybit-hourly-momentum` and `terminal-hourly-momentum` now share one factored-out `strategies/lib/momentum.ts`; `deriv-synthetic-meanrev`; `polymarket-sim-longshot-fade`, sharing `strategies/lib/longshotFade.ts` with `manifold-longshot-fade`). Credentials manager (`/api/alpha/credentials` + `/alpha` UI) is real UI/schema plumbing but explicitly NOT wired to actual Supabase Vault — `secret_ref` is a `vault:pending:<uuid>` placeholder, since there's no live Vault to write to here; a submitted secret is used only to compute a short confirmation prefix and is never persisted, logged, or echoed back. 217 Vitest tests (201 pass + 16 skip — Deriv/Kalshi/Bybit skip on missing credentials, Manifold's 4 still skip too, Polymarket's now genuinely run), `tsc`/`eslint`/`playwright` (5/5)/build all green.

- Phase 5 (final core phase): `budgetRule.ts` (written and unit-tested directly, not delegated — the money-adjacent reallocation math) implements the conservative sample-size-aware bandit: equal split while under `min_samples` (50), proportional to `max(0, lower ROI CI bound)` once mature, auto-pause on a negative upper CI bound, per-strategy budget floor always respected, reallocation pools scoped per user. `evaluate.ts` rewritten from Phase 1's null-writing stub to real Level 2 scoring (ROI-CI/hit-rate/drawdown/Sharpe via `bootstrapRoiCI` etc. from `football/metrics.ts`, reused generically despite the directory name) — verified the per-venue CLV conversion is correct against every adapter's own `place()` code (`zoqo-sportsbook` stores decimal odds needing `1/x`; `manifold`/`kalshi-demo`/`polymarket-sim` already store an implied probability; `zoqo-terminal`/`bybit-demo`/`deriv-virtual` have no honest probability reading and correctly get `null` CLV rather than a fabricated number). RPS/Brier computed only for `zoqo-sportsbook` (the one venue with a recoverable outcome + logged `model_prob`), RPS specifically undercounted by design rather than guessing missing legs of the 1X2 distribution. Weekly walk-forward param search (`paramSearch.ts`, `terminal-hourly-momentum` is the one strategy with a declared `paramSpace` so far) proposes via `alpha_events`, never self-applies — `apply_proposal` (ownership-checked, same pattern as everywhere else) is the only path from proposal to live params. `/alpha` proposals inbox, 4 new MCP tools (`list_proposals`/`apply_proposal`/`get_strategy_stats`/`get_health`), and a daily-digest Alpha section (degrades gracefully like the rest of that route). 245 Vitest tests (229 pass + 16 skip), `tsc`/`eslint`/`playwright` (5/5)/build all green.

## Next

- **Deploy the code.** `git push` + Vercel deploy (or however this project normally ships) so the `alpha-*` routes stop 404ing against the live cron jobs already scheduled and running. This is the one thing standing between "infra is live" and "the loop actually runs." Ask before pushing, per the build prompt's standing rule.
- Once deployed: create a real strategy (e.g. `terminal-hourly-momentum` on `zoqo-terminal`) via `/alpha` or the MCP and enable it + the venue, to see a real scheduled run happen end to end (currently `alpha_strategies` is empty, so `alpha-run`/`alpha-settle`/`alpha-evaluate` will keep ticking as harmless no-ops).
- Get the venue/data API keys (Manifold first — it's the fastest path to a fully real external bet) to move adapters from "built against docs" to "verified live."
- Wire `/api/alpha/credentials`'s `secret_ref` to a real Supabase Vault secret now that a project is linked (currently an opaque placeholder — see Phase 4's entry above).
- Confirm the Vercel plan tier (still unconfirmed) — matters for whether the existing `vercel.ts` per-minute entries do anything; pg_cron is the real scheduler regardless and is already proven live.

## Needs Aise (human-only, exact URLs)

1. ~~Link the Supabase project and apply the migration~~ — **done**, see the environment update above.
2. ~~Fill in and run `supabase/alpha-cron-apply.sql.example`~~ — **done**, all 5 jobs scheduled and confirmed ticking live.
3. **Confirm the Vercel plan tier** for the `zoqo` project (`aiseosauyiidahorgmailcoms-projects` team) — the CLI's project-level commands available here don't surface it. pg_cron is the real scheduler regardless (already proven live), so this only affects the pre-existing `vercel.ts` per-minute entries.
4. **Configure Supabase custom SMTP** (existing TODOS item, blocks new sign-ups in prod at the default 2 emails/hour cap) — Supabase dashboard → Project Settings → Auth → SMTP Settings.
5. **Deploy the built code** (`git push` to whatever remote/branch this project ships from, then the normal Vercel deploy path) — the live cron jobs are already hitting `/api/cron/alpha-*` and currently 404ing because this is the one piece still sitting only in local commits.
6. **Obtain free API keys** and add them as Vercel env vars + `.env.local` for local dev:
   - Manifold: https://manifold.markets/ → profile → API key → `MANIFOLD_API_KEY`.
   - Kalshi demo: https://demo.kalshi.co (separate account from production Kalshi).
   - Bybit demo: https://api-demo.bybit.com (testnet/demo account creation).
   - Deriv: https://developers.deriv.com/ → register an app, get a virtual-account API token.
   - odds-api.io: https://odds-api.io/ → free tier signup (choose Bet9ja + SportyBet as the 2 free books) → `ODDS_API_IO_KEY`. **Once obtained, verify `src/lib/alpha/providers/oddsApiIo.ts`'s endpoint/response shape against the real API** — that file's header is explicit that its request/response shape is an educated guess (the near-universal bookmakers[]→markets[]→outcomes[] convention), not verified against a live key; only `normalizeOddsEnvelope` and the request params in `fetchOddsSnapshots` should need to change if it differs.
   - API-Football: https://www.api-football.com/ (api-sports.io) → free tier signup → `API_FOOTBALL_KEY`. This one's endpoint shape (v3, documented at api-football.com/documentation-v3) is used with much higher confidence since it's a stable public API, but is still unverified live in this environment.
7. **Rotate the Supabase CLI personal access token** used to apply this migration, out of caution — it appeared in this chat's transcript rather than only in a private shell command. Revoke/regenerate at https://supabase.com/dashboard/account/tokens.
